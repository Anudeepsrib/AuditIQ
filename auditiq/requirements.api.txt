# ── AuditIQ API Dependencies ─────────────────────────────────
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.2.0

# Database
sqlalchemy>=2.0.35
alembic>=1.13.0

# Authentication (pinned for bcrypt/passlib compatibility on py3.11+)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # avoid 4.1+ __about__ and 72-byte issues with some passlib combos; upgrade after testing

# Middleware
slowapi>=0.1.9
structlog>=24.4.0

# MLflow (client only for API)
mlflow>=2.16.0

# Testing
pytest>=8.3.0
httpx>=0.27.0
